# Shared environment configuration for the Mini AiMan stack.
# Copy this file to `.env.local` for the Next.js frontend (loaded by Next automatically)
# and to `python_backend/.env` if you prefer dotenv loading there. Replace placeholder
# values with the credentials from your LiveKit project.

# LiveKit server that issues access tokens (frontend API route + backend worker)
LIVEKIT_URL=https://your-project-id.livekit.cloud
LIVEKIT_API_KEY=lk_api_key_example
LIVEKIT_API_SECRET=lk_api_secret_example

# Agent identity the frontend requests; keep in sync with python_backend/main.py
# (MiniAimanAgent defaults to "mini-aiman-python")
NEXT_PUBLIC_LIVEKIT_AGENT_NAME=mini-aiman-python

# Frontend overrides (optional)
# NEXT_PUBLIC_APP_CONFIG_ENDPOINT=https://example.com/app-config.json
# NEXT_PUBLIC_CONN_DETAILS_ENDPOINT=https://example.com/api/connection-details
# SANDBOX_ID=aiman-dev

# Backend provider credentials (required for python_backend)
# OPENAI_API_KEY=sk-...
# TRIGGER_PHRASES=hey aiman,aiman
