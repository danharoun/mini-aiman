# Unified environment configuration for the Mini AiMan stack.
# Copy this file to `.env` (used by docker-compose and local backend runs).
# Optionally duplicate it to `.env.local` if you want Next.js to pick up the same values when running outside Docker.

# LiveKit server credentials used by both the frontend API route and the Python agent.
LIVEKIT_URL=https://your-project-id.livekit.cloud
LIVEKIT_API_KEY=lk_api_key_example
LIVEKIT_API_SECRET=lk_api_secret_example

# Agent identity the frontend requests; keep this in sync with python_backend/main.py.
NEXT_PUBLIC_LIVEKIT_AGENT_NAME=mini-aiman-python

# --- Frontend (optional) ----------------------------------------------------
# Uncomment and adjust if you host remote config/token endpoints.
# NEXT_PUBLIC_APP_CONFIG_ENDPOINT=https://example.com/app-config.json
# NEXT_PUBLIC_CONN_DETAILS_ENDPOINT=https://example.com/api/connection-details
# SANDBOX_ID=aiman-dev

# --- Backend / LLM configuration --------------------------------------------
OPENAI_API_KEY=sk-your-openai-key
OPENAI_REALTIME_MODEL=gpt-realtime
OPENAI_REALTIME_VOICE=marin
TRIGGER_PHRASES=hey aiman,aiman
LOG_LEVEL=INFO
